# Security Configuration for Math Mastery Pro / Lab_OS
# 
# Credentials are now stored in /app/api/config/route.ts
# For production, set these environment variables to override the defaults:

# YouTube API Key for MEDIA_SCRAPER feature
YOUTUBE_API_KEY=AIzaSyBReeUyH3K8q5r8hQAGjEVt-J9U4DVCtNY

# User Credentials - JSON object with username: password pairs
# Admin users (valens, googlechroma) have access to ADMIN_PANEL
AUTH_USERS_JSON={"valens":"kjwhrkuwer9834234","googlechroma":"chromaAdmin2024!","c31ws":"oprekworkl829321394","c31nk":"q234532905uoiuriowe","c31xy":"wkehr324rwer","admin123":"admin123"}

# How to use:
# 1. Set these in your Vercel dashboard or .env.local file
# 2. The API route at /api/config will use env vars if set, otherwise falls back to defaults in route.ts
# 3. Never commit .env.local to version control
